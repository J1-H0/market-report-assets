# slots.json 스키마 — 마감 리포트 렌더러 입력

`mkreport.py`가 읽는 입력 형식이다. **없는 값은 `null`로 두고 키 자체를 빼지 않는다.**
`null`은 `[미확보]`로 렌더되며 근사치로 대체하지 않는다.

## 공통 규칙

- 색·막대폭·부호 표기·푸터는 전부 코드가 정한다. **색 hex를 직접 쓰지 않는다.**
- 등락(`chg`)은 숫자만 넣는다. `+`/`%`는 코드가 붙인다. 10년물 등 bp 단위는 `"unit":"bp"`를 함께 준다.
- 표 셀은 문자열이거나 `{"t":"텍스트","chg":수치}`. 후자면 부호에 따라 색이 붙는다. `{"t":null}`은 `[미확보]`.
- `footer_status`의 `{MISSING}`은 코드가 미확보 건수로 바꾼다.
- `value`가 `null`이면 등락 칸은 **빈 칸**이 된다. `[미확보]`를 한 칸에 두 번 찍지 않는다.
- **등락 개념이 없는 타일에는 `"nodir": true`를 단다.** 거래대금·수급 금액처럼 `chg`가 아예
  없는 값이다. 두 가지가 달라진다 — ① `chg` 없음을 미확보로 세지 않는다(푸터 MISSING 정확해짐)
  ② 방향 충돌 검사에서 제외된다('외국인 매도에 지수가 하락' 같은 문장을 부호 충돌로 오검출하지 않음).
- **타일 아랫줄을 등락률이 아닌 다른 글자로 쓰려면 `"sub"`를 준다.** 그 문자열이 등락 자리에
  그대로 들어간다(예: `"억원 순매수"`, `"공포"`). 색은 여전히 `chg` 부호가 정한다.
- **부호 개념이 없는 지표에는 `"tone": "flat"`을 단다.** 색이 회색(`#6b6963`)으로 고정되고
  상승 빨강·하락 파랑이 붙지 않는다. Fear&Greed가 여기 해당한다.
- **`"digits": 1`** 을 주면 등락률을 소수 1자리로 찍는다(기본 2자리). WTI가 여기 해당한다.
- **`value`를 `""`(빈 문자열)로 두면 `[미확보]`가 아니라 빈칸이 된다.** '수집 실패'가 아니라
  '해당 없음'을 뜻한다(국내 휴장일의 수급 타일). 이때도 본문에서 그 대상은 언급할 수 없다.
- **미국 10년물은 슬롯 하나다.** `tiles`의 10년물이 정본이고, `rate_tiles`의 10년물 값·등락은
  코드가 거기서 복사해 덮어쓴다(양쪽이 다른 값으로 나가던 사고 방지). `rate_tiles` 쪽은
  `{"label":"미국 10년물","value":null,"chg":null,"unit":"bp"}`로 두면 된다.
- **`consensus`에서 현재가가 `null`인 행은 코드가 통째로 뺀다.** Upside를 낼 수 없어 행이 무의미하다.
  현재가 출처는 아래뿐이다(FMP `quote`는 플랜에서 막혀 있다).

  | 리포트 | 출처 | 확정 종가 판정 |
  |---|---|---|
  | 미국장 | 시트A `컨센서스 시세` 탭 / 시트B 대형주 18종 | 탭의 기준시각이 `대상일 16:0x`(ET) |
  | 국내장 | **`한국 시총상위`** `1_PvZ40WVkHqq9qRiEcOD0XFvDQFMMknQ_tWE3BmCLZ4` — 탭 **둘 다** | 기준시각 날짜 = 대상일 |

  `한국 시총상위`는 탭이 둘이고 `read_file_content`가 표 두 개로 돌려준다.
  ① **첫 표(시총 상위)** — 앱스스크립트가 네이버 시총 랭킹에서 매시간 다시 쓴다. 코스피 100 +
     코스닥 100, ETF·우선주 제외. 열은 `시장 | 순위 | 종목코드 | 종목명 | 종가 | 전일대비 | 등락률 | 거래대금_억 | 시총_억 | 기준시각`.
     ★매시간 통째로 교체되므로 여기에 손으로 종목을 넣으면 지워진다.★
  ② **둘째 표(`추가 종목`)** — 시총 200위 밖인데 목표가 리포트가 나오는 종목을 손으로 넣는 곳.
     A열에 6자리 종목코드만 적으면 나머지가 채워지고, 지워지지 않는다.
     열은 `종목코드 | 종목명 | 종가 | 전일대비 | 등락률 | 기준시각 | 갱신시각`.

  현재가는 **두 표 어디서든** 찾으면 된다. 첫 표는 추가로 **'시총 상위 200종목 중 최대 상승/하락'
  범위 확인**에도 쓴다(미국장의 시트B 역할) — 이 판정에는 둘째 표를 섞지 않는다.
  두 표 어디에도 없는 종목은 컨센서스 표에 올리지 말고 `cons_note` 산문으로만 다루되,
  그 종목명을 [종료] 로그에 남겨 `추가 종목` 탭에 넣을 수 있게 한다.
- **최상급·기록 표현을 쓰려면 `superlative_checks`에 근거를 남겨야 한다.** 없으면 GATE HOLD다.
  ```json
  "superlative_checks": [
    {"expr": "사상 최고", "basis": "9/19 종가 27,043.11 대비 9/21 종가 27,122.09로 직전 최고치 경신(시트A 이력 확인)"}
  ]
  ```
  `basis`는 10자 이상이어야 하고, '기사/헤드라인/제목/보도/전망/임박/관측/예상' 같은 말이 들어가면
  근거로 인정되지 않는다. 기사 제목이 유일한 근거면 표현 자체를 빼는 것이 맞다.

## 미국장 (`"market":"us"`)

**`tiles` 8개 — 순서 엄수** (2026-09-23 확정)
1행 `S&P 500 · 나스닥 · 다우존스 · 필라델피아 반도체`
2행 `미국 10년물 · WTI · 원/달러 · Fear&Greed`

- **WTI** — 값은 `"$89.69"` 형식, 등락은 `"digits": 1`로 소수 1자리(`-6.4%`).
  시트A의 `WTI` 행이 정본이고, 없으면 FMP commodity. 둘 다 없으면 `value: null` 한 번만.
- **Fear&Greed** — `value`는 점수(정수 문자열), `sub`는 등급
  (`극단적 공포`·`공포`·`중립`·`탐욕`·`극단적 탐욕`). 부호가 없는 지표이므로
  ★`"tone": "flat"`으로 회색 고정★ — 상승 빨강·하락 파랑을 쓰지 않는다.
  대상일 확인분이 없으면 `value: null`.
- **러셀2000·VIX는 타일이 아니라 `aux`(섹션 1 하단 보조지표 줄)에 있다.**
  `aux`는 `VIX · 러셀2000 · GLD · TLT` 4개 고정이다.
- **DXY(달러지수)는 리포트에서 완전히 뺐다.** 타일·보조지표·수집 대상 어디에도 없다.
  FMP `DXUSD`가 플랜에서 막혀 있어 매번 [미확보]로 나가던 자리다. 다시 넣지 말 것.

```json
{
  "market": "us",
  "date": "2026-09-18",
  "date_label": "09/18(금)",
  "issue_no": 137,

  "headline": "한 문장. 지수 방향 + 원인 + 가장 큰 개별 재료",
  "subtitle": "정규장 확정 종가(16:00 ET) 기준 · …",
  "key_points": ["…", "…", "…"],
  "summary_prose": "4문장. ①원인(매크로) ②결과(지수·섹터) ③심리(VIX·F&G) ④특이 종목",

  "tiles": [
    {"label": "S&P 500", "value": "7,650.50", "chg": 0.17},
    {"label": "나스닥", "value": "26,522.55", "chg": 0.39},
    {"label": "다우존스", "value": "51,682.64", "chg": -0.18},
    {"label": "필라델피아 반도체", "value": "11,921.69", "chg": 2.78},
    {"label": "미국 10년물", "value": "4.998%", "chg": 5.1, "unit": "bp"},
    {"label": "WTI", "value": "$93.14", "chg": 0.83, "digits": 1},
    {"label": "원/달러", "value": "1,384.00", "chg": 0.21},
    {"label": "Fear&Greed", "value": "34", "chg": null, "sub": "공포",
     "tone": "flat", "nodir": true}
  ],

  "aux": [
    {"label": "VIX", "value": "14.81", "chg": -4.08},
    {"label": "러셀2000", "value": "2,860.40", "chg": -0.50},
    {"label": "GLD", "value": "401.17", "chg": 0.71},
    {"label": "TLT", "value": "81.25", "chg": -0.65}
  ],

  "section2_title": "섹터 · 종목",
  "sector_note_top": "11개 섹터 ETF 등락률 · 내림차순",
  "sectors": [
    {"name": "기술 XLK", "chg": 0.82},
    {"name": "산업재 XLI", "chg": 0.44}
  ],
  "sector_footnote": "시장 폭 2/11 · SMH/SPY 상대강도 5일 +1.13%p · 20일 +1.96%p · SMH σ강도 0.81",
  "movers": {
    "head": [["상승 상위", "left"], ["등락", "right"], ["하락 상위", "left"], ["등락", "right"]],
    "rows": [
      ["ARM", {"chg": 4.04}, "퀄컴", {"chg": -5.82}],
      ["마이크론", {"chg": 3.92}, "넷플릭스", {"chg": -4.67}]
    ]
  },
  "sector_note": "2~3문장. 표에서 실제로 상위·하위인 섹터가 왜 움직였는지",

  "rate_tiles": [
    {"label": "미국 10년물", "value": null, "chg": null, "unit": "bp"},
    {"label": "미국 2년물", "value": "4.760%", "chg": 9.0, "unit": "bp"},
    {"label": "2s10s 스프레드", "value": "+25bp", "chg": -3.9, "unit": "bp"}
  ],
  "macro_rows": [
    {"k": "금리", "v": "…"},
    {"k": "심리", "v": "…"},
    {"k": "안전자산", "v": "…"}
  ],
  "consensus": {
    "head": [["종목", "left"], ["투자의견", "left"], ["목표가", "right"],
             ["현재가", "right"], ["Upside", "right"], ["핵심", "left"]],
    "rows": [
      ["마이크론", "매수 유지", "1,180.00", "1,015.80", {"t": "+16.2%", "chg": 16.2}, "HBM 공급 계약 확대"]
    ]
  },
  "superlative_checks": [],
  "cons_footnote": "Upside = 목표가 ÷ 현재가 − 1. 현재가는 정규장 확정 종가.",
  "cons_note": "…",

  "section4_title": "국내 read-through",
  "s4_tiles": [
    {"label": "EWY 한국 ETF", "value": "181.31", "chg": -0.59},
    {"label": "TSMC", "value": "434.67", "chg": 1.02},
    {"label": "쿠팡", "value": "14.29", "chg": -1.24},
    {"label": "외국인 순매수 (전일 코스피)", "value": "+4,201", "chg": 4201,
     "sub": "억원 순매수", "nodir": true}
  ],
  "map_head_1": "미국 동향",
  "map_head_2": "국내 대응",
  "map_rows": [
    {"a": "메모리 강세 (MU +3.92%, SMH +2.21%)", "b": "삼성전자 · SK하이닉스", "dir": "긍정"}
  ],
  "s4_footnote": "…",

  "calendar_groups": [
    {"group": "경제 · 증시", "rows": [
      {"d": "09/21(월)", "t": "미국 8월 경기선행지수"}
    ]}
  ],
  "news": [
    {"title": "…", "url": "https://…", "date": "9/18"}
  ],

  "deep_dive": {
    "title": "…",
    "prose": "1단락",
    "rows": [
      {"k": "근거", "v": "…"},
      {"k": "트리거", "v": "이 논리가 맞는지 확인되는 다음 이벤트·수치"},
      {"k": "포인트", "v": "…"}
    ]
  },

  "source_note_1": "지수·섹터는 정규장 확정 종가 기준.",
  "s5_footnote": "일정은 확정 공시로 확인된 것만.",
  "footer_status": "XCHK n/n · MISSING {MISSING}"
}
```

## 국내장 (`"market":"kr"`)

밴드 색(`#0F3D3E`)만 바뀌고 구조는 같다. 다른 부분은 아래뿐이다.
회귀 픽스처: `slots_kr_0922_test.json`(GATE PASS · MISSING 0 기대).

**`tiles` 8개** — 코스피 · 코스닥 · 원/달러 · 코스피 거래대금 · 외국인 · 기관 · 개인 · 미국 10년물.
뒤 넷 중 거래대금·외국인·기관·개인은 등락 개념이 없으므로 ★`"nodir": true`★ 를 단다.
미국 10년물은 노션 DB에서 읽은 값을 인용한다(미국장 루틴이 기록한 것).

```json
{"label": "코스피",          "value": "7,017.91",  "chg": 0.15},
{"label": "코스피 거래대금",  "value": "22.5조",    "chg": null, "nodir": true},
{"label": "외국인",          "value": "+789억",    "chg": null, "nodir": true},
{"label": "미국 10년물",     "value": "4.963%",    "chg": -3.5, "unit": "bp"}
```

**`aux` 3개** — 간밤 미국(S&P500 · 나스닥 · SOX). 전부 노션 DB 인용값이다.

**섹션 2 `"업종 · 수급"`** — `sectors`의 `chg`에 **등락률이 아니라 상대강도(%p)** 를 넣는다.
상대강도 = 업종 등락률 − 시장 등락률. 상위 5 + 하위 5, 총 10개를 내림차순으로.
막대 길이·색은 이 값 기준으로 그려지므로 '시장을 얼마나 앞섰나'가 그대로 보인다.
`sector_note_top`에 무엇을 그린 막대인지 반드시 밝힌다.
`movers` 표는 같은 업종의 `업종 | 등락률 | 외국인(억) | 기관(억)`.

**섹션 4 `"수급 · 내일 관전"`** — `flow` 표(수급)를 추가로 쓴다. 형식은 `movers`와 같다.
`{"head": [[…]], "rows": [[…]]}` — 있으면 렌더되고 없으면 그 블록이 통째로 빠진다.
`map_head_1` = `"관전 포인트"`, `map_head_2` = `"판단 기준 수치"`.
`s4_tiles`는 낼 수 있는 값이 있을 때만 쓴다. 외국인 5일 누적은 수급 시트가 당일 1행만
남기고 덮어쓰므로 ★산출하지 않는다★ — 빈 타일을 만들지 말고 슬롯을 비운다.

```json
"flow": {
  "head": [["구분","left"], ["코스피(억)","right"], ["코스닥(억)","right"]],
  "rows": [["외국인", {"t":"+789","chg":789}, {"t":"-573","chg":-573}]]
}
```

**`calendar_groups`는 두 갈래** — `"경제 · 증시"`와 `"엔터 일정"`.

**컨센서스 현재가** — 한국 종목은 `한국 시총상위` 파일에서 가져온다(아래 참조).

## 게이트 출력 읽는 법

`python3 mkreport.py slots.json .` 실행 시 표준출력:

```
GATE HOLD · MISSING 3
NOTE:  10년물 슬롯 정합: 4.960% -5.0 -> 4.963% -3.5 (정본 상단 타일)
NOTE:  컨센서스 행 제외(현재가 미확보): 마벨, 델 테크놀로지스
BLOCK: 발표 요일 오류: '신규 실업수당'는 목요일 발표인데 09/25(금)에 적혔다
BLOCK: 미확보 슬롯 'WTI'를 본문이 언급: 유가
BLOCK: 내부 사정 노출: 시트B
BLOCK: 최상급 근거 미기재(삭제하거나 superlative_checks에 근거를 적을 것): …사상 최고…
WARN:  타일 미확보 1건: WTI → 제목 꼬리표 필요
LEFTOVER_SLOTS: 0
LEGACY_BACKGROUND_SHORTHAND: 0
WROTE: ./body.html 36472 bytes, ./body.txt 5447 bytes
```

종료 코드 — **0 = PASS(초안 작성), 3 = HOLD(초안 금지), 2 = 사용법 오류.**

- `BLOCK:` 이 한 줄이라도 있으면 **초안을 만들지 않는다.** slots.json을 고쳐 재실행한다.
  고칠 방법은 하나뿐이다 — 틀린 문장·행·표현을 지우거나 근거를 채우는 것. 게이트를 피해
  표현만 돌려 쓰지 않는다.
- `WARN:` 은 수집 실패다. 막지 않되 제목에 꼬리표('(지수 확정치 지연)' 등)를 붙이고 푸터 MISSING에 남긴다.
- `NOTE:` 는 코드가 자동으로 손본 내용이다. 그대로 두면 된다.
- `LEFTOVER_SLOTS`가 0이 아니면 slots.json에 키가 빠진 것이다.
- `LEGACY_BACKGROUND_SHORTHAND`가 0이 아니면 템플릿에 CSS `background:` 단축 속성이 남아 있다.
  Gmail이 저장 시점에 지우므로 반드시 0이어야 한다(밴드·막대가 통째로 사라진다).

### BLOCK 사유 목록 (전부 코드로 강제)

| 사유 | 내용 |
|---|---|
| 방향 충돌 | 본문이 '상승'이라 썼는데 슬롯 부호가 음수인 경우(및 반대) |
| 캘린더 요일 불일치 | `MM/DD(요일)`의 요일이 실제 요일과 다름 |
| 발표 요일 오류 | 주간 실업수당=목, EIA 원유재고=수, 천연가스 재고=목 |
| 추측성 표현 | 제목·키포인트에 '약 ','추정','근사','가량','안팎','내외' |
| 뉴스 발행일 | 대상일 또는 −1일이 아닌 것, URL에 박힌 날짜와 표기 발행일 불일치 |
| 뉴스 중복 | 제목 토큰 50% 이상 겹치는 두 건 |
| 미확보 슬롯 언급 | `null`·`""` 슬롯의 대상(WTI→유가·원유, 외국인→외인 …)을 본문 판단 문장에서 언급 |
| 내부 사정 노출 | 시트A/B/C, 노션, 프록시, stale, 백필, EDGAR, 게이트, 미산출 |
| 금지어 사용 | `시금석` — 폐기한 말이다. 공부섹터 3행 라벨·본문 모두 `트리거`를 쓴다 |
| 최상급 근거 미기재 | `superlative_checks`에 근거가 없는 최상급·기록 표현 |
